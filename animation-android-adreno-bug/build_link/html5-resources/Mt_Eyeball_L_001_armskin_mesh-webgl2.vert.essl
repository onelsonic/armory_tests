#version 300 es

uniform vec4 skinBones[320];
uniform float texUnpack;
uniform float posUnpack;
uniform mat3 N;
uniform mat4 W;
uniform mat4 WVP;

in vec4 pos;
out vec2 texCoord;
in vec2 tex;
in vec4 bone;
in vec4 weight;
out vec3 wnormal;
in vec2 nor;
out vec3 wposition;

void getSkinningDualQuat(ivec4 bone_1, inout vec4 weight_1, inout vec4 A, inout vec4 B)
{
    uvec4 bonei = uvec4(bone_1 * ivec4(2));
    mat4 matA = mat4(vec4(skinBones[bonei.x]), vec4(skinBones[bonei.y]), vec4(skinBones[bonei.z]), vec4(skinBones[bonei.w]));
    mat4 matB = mat4(vec4(skinBones[bonei.x + 1u]), vec4(skinBones[bonei.y + 1u]), vec4(skinBones[bonei.z + 1u]), vec4(skinBones[bonei.w + 1u]));
    vec3 _130 = weight_1.xyz * sign(matA[3] * matA).xyz;
    weight_1 = vec4(_130.x, _130.y, _130.z, weight_1.w);
    A = matA * weight_1;
    B = matB * weight_1;
    float invNormA = 1.0 / length(A);
    A *= invNormA;
    B *= invNormA;
}

void main()
{
    vec4 spos = vec4(pos.xyz, 1.0);
    texCoord = tex * texUnpack;
    vec4 param = weight;
    vec4 skinB;
    vec4 param_2 = skinB;
    vec4 param_1;
    getSkinningDualQuat(ivec4(bone * 32767.0), param, param_1, param_2);
    vec4 skinA = param_1;
    skinB = param_2;
    vec3 _189 = spos.xyz * posUnpack;
    spos = vec4(_189.x, _189.y, _189.z, spos.w);
    vec3 _210 = spos.xyz + (cross(skinA.xyz, cross(skinA.xyz, spos.xyz) + (spos.xyz * skinA.w)) * 2.0);
    spos = vec4(_210.x, _210.y, _210.z, spos.w);
    vec3 _233 = spos.xyz + ((((skinB.xyz * skinA.w) - (skinA.xyz * skinB.w)) + cross(skinA.xyz, skinB.xyz)) * 2.0);
    spos = vec4(_233.x, _233.y, _233.z, spos.w);
    vec3 _240 = spos.xyz / vec3(posUnpack);
    spos = vec4(_240.x, _240.y, _240.z, spos.w);
    wnormal = normalize(N * (vec3(nor, pos.w) + (cross(skinA.xyz, cross(skinA.xyz, vec3(nor, pos.w)) + (vec3(nor, pos.w) * skinA.w)) * 2.0)));
    wposition = vec4(W * spos).xyz;
    gl_Position = WVP * spos;
}

